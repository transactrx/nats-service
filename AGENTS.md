# Repository Instructions

- Format all Go code using `gofmt -w` before committing.
- When Go dependencies are already installed, run `go vet ./...` and `go test ./...` after making changes. If these commands fail due to missing dependencies or network restrictions, document this in the PR Testing section.
- Summaries for pull requests should mention major file changes.
